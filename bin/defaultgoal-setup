#!/bin/bash

set -aeu
source _shellflags

case "$TESTCONFIG" in
    /*) true ;;
    *) TESTCONFIG="$(pwd)/$TESTCONFIG"
esac

makecall -C "${TESTROOT}/components/setup/default" TESTCONFIG="$TESTCONFIG" setup