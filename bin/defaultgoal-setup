#!/bin/bash

set -aeu
source _shellflags

case "$TESTCONFIG" in
    /*) true ;;
    *) TESTCONFIG="$(pwd)/$TESTCONFIG"
esac
checktestconfig || exit 1

# find all setup requirement
unset TESTSUITE_TESTS

makecall -C "${TESTROOT}/components/setup/default" TESTCONFIG="$TESTCONFIG" setup